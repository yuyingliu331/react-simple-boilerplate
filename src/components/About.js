import React, {Component} from 'react';

export default class Login extends Component{
    render(){
        return(
            <h2>This is the only socail app that you will need.</h2>
        );
    }

}